# Задание Codex № 06 — проверка исправлений по вердикту № 05

**Скопируйте текст ниже целиком в Codex.**

---

Ты работаешь в каталоге `/Users/nikitamakarenko/Documents/All3 Execution/Sales Platform 2.0`. Прочитай `docs/audit/verdicts/PROTOCOL.md`.

## Контекст

Твой вердикт `docs/audit/verdicts/codex-prototype-260805.md` отклонил прототип: 13 расхождений чисел, 7 провалов разметки, четыре дефекта класса P0 в журнале событий. Исполнитель заявляет: **все 15 дефектов закрыты** (коммит «Вердикт Codex № 05 отработан»). Твоя задача — проверить, что исправления реальны, а не заявлены. Класс «исправляется заявление, а не объект» убил в этом проекте шесть партий; проверяй объекты.

Метод тот же, что в № 05: копия в `/tmp`, SSR-снимки через `renderToString`, прямые вызовы store под vitest. Оригиналы не менять. Отчёт исполнителя (сообщение коммита) читай **после** того, как составишь своё мнение.

## Пятнадцать пунктов — проверить каждый

**Журнал (M-4):**

1. `useStore.setState` больше не существует в публичном API — и это единственный был обход. Проверь не только отсутствие setState: **поищи новые обходы** (экспорт сырого store, мутация полученных из `getState()` объектов, запись в `snapshots`/`journal` напрямую).
2. Undo с курсором: два undo отменяют два разных события; третий — no-op; `undoOf` различается. Затем **композиция, которой в тестах нет**: undo → новое событие → undo → undo. Что происходит с курсором, когда после отмены появляется новое событие?
3. Разрешение конфликта WFL — одно событие; отмена восстанавливает значение, provenance и статус конфликта целиком.
4. Непринятый кандидат хранится со `selectionStatus: 'alternative'`.

**Снапшот (M-3):**

5. `sendOffer` создаёт снапшот с точным итогом, флагом Regionalfaktor, покрытием и позицией журнала. Проверь неприкосновенность: есть ли путь изменить или удалить снапшот после создания? Событие отправки не имеет inverse — значит `undo` после отправки должен отменять предыдущее событие, а не отправку. Проверь.

**Честность симуляции:**

6. `Zugestellt` помечен как симулированное подтверждение в видимом тексте.
7. PDF несёт бейдж `Muster`; preflight называет вложения заглушками прототипа.

**Числа и копия:**

8. Маржа, шесть драйверов риска, потолок, балконная доля, Δ класса — теперь из `catalog.json.internalConfig`, который построитель извлекает из `calculation-spec.md` §1.1 и `t0-fallback-rules.md`. Проверь цепочку: измени значение в исходном md на копии → `python3 tools/build_fixtures.py` → значение в JSON меняется; экраны читают JSON, не константы.
9. Annahmen в S2 P4 дословны источнику; подстановка значения в слот GK **названа** в UI-тексте.

**Разметка:**

10. Герой S3: `€` отделён символом U+202F внутри текста, не CSS-отступом.
11. Шаблонный литерал `'${NNBSP}'` из разметки исчез (глава 2 и глава 4 S3).
12. Карточка S1: role, tabIndex, обработчик Enter/Space, видимый фокус.
13. Поле ввода: минимальная высота — токен цели нажатия 44 px.
14. Таблицы S6 несут `<caption>`; ставки риска — узкий неразрывный.
15. `npm run verify` — 0 новых нарушений (пробелы в файле задания № 05 исправлены).

## Сверх пятнадцати

Как всегда: самое ценное — то, чего в списке нет. Исправления могли сломать соседнее — проверь, что 54 теста не ослаблены (сравни с прежними 50: что добавлено, что переписано, не удалён ли неудобный тест), и что SSR-снимки экранов не потеряли содержимое против твоих прошлых снимков.

## Формат отчёта

Файл `docs/audit/verdicts/codex-prototype-recheck-260805.md`. Первая строка:

```
ВЕРДИКТ: ПРИНЯТО/ОТКЛОНЕНО · исправлений подтверждено N из 15 · новых дефектов K
```

Далее таблица по 15 пунктам (пункт · подтверждающее наблюдение · да/нет), новые дефекты с адресами, и раздел «что по-прежнему проверяется только в браузере».
