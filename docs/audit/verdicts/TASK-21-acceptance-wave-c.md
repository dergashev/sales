# Задание Codex № 21 — приёмка волны C и повтор UX-оценки

**Черновик. Передавать, когда Claude сообщит: объект приёмки должен быть неподвижен (`PROTOCOL.md` §2-ter).**

**Скопируйте текст ниже целиком в Codex.**

---

Ты работаешь в каталоге `/Users/nikitamakarenko/Documents/All3 Execution/Sales Platform 2.0`. Зоны — `PROTOCOL.md` §2-bis. Это приёмка: `src/**` только читается, дефекты называются адресами. На время прохода Claude в `src/**` не пишет — если увидишь коммиты в нём датой прохода, останови приёмку и скажи об этом: вердикт по движущемуся объекту недействителен.

## A. Приёмка того, что появилось после № 17

Пять блоков. По каждому — принят / отклонён / принят с оговоркой, с проверкой механизма, а не текста коммита.

1. **Надбавки за риск и третий уровень KG (`D-23`).** Надбавка обязана считаться от СВОЕЙ базы: `KG 320` = 11 % от KG 300, `4 %` от неё, — а не от KG 300 целиком. Проверь, что разбиение 70/22/8 берёт итог **до** надбавок (иначе надбавка растворяется в KG 300 и увеличивает собственную базу), что сумма подгрупп равна группе, и что применение создаёт событие с дельтой и отменяется. **Умолчание «выключено» — решение PO от 07.08 («оставляем решением продавца»), не дефект.**
2. **Изображения карточек (`TASK-18`).** Все 43 на месте в сборке; соответствие берётся из манифеста, не из шаблона имени; битая картинка снимает слот, а подпись и цена остаются (правило 8); `alt` пустой намеренно. Проверь также вес страницы и то, что плитки не поехали на узких ширинах.
3. **Сравнение вариантов бок о бок** (`VariantsSideBySide` в `src/screens/OptionChapter.tsx`). Число «Angebotssumme danach» обязано совпадать с текущим итогом плюс последствие плитки — второго способа посчитать быть не должно.
4. **Регрессии № 17.** Дельта-чип, призрак и тост: класс состояния на постоянном элементе, движение из системы, слот не сдвигает вёрстку при появлении двухстрочного содержимого. Проверь в **фоновой вкладке** — исходный дефект был именно там.
5. **Компоненты, которые Claude добавит до передачи задания** (гейт-диалог DC-33, Speicher-Chip DC-30, заметки DC-43, туры DC-14): полнота `Classes:`, клавиатура, семь состояний, поведение в презентации (`NOTE-006`: заметок в клиентских профилях не существует).

## B. Повтор UX-оценки — третий замер

Те же 14 поверхностей, те же шесть измерений, та же шкала. Прошлые счёты: **3 из 14** (ревью № 13) → **5 из 14** (приёмка № 17). Назови новый счёт и по каждой сменившей вердикт поверхности — механизм, который его сменил. Отдельно: главы 1 и 3 были «налоговой формой», и Claude работает над ними по твоим же трём исправлениям на каждую — проверь результат против того, что ты просил.

## C. EN после поставки № 4

Если `TASK-19` к этому моменту принята: пройди золотой путь на EN и назови **каждую** оставшуюся немецкую строку с причиной (нормативное имя / значение фикстуры / содержимое артефакта по `D-13`). Пометка «EN · Entwurf» снимается только когда список пуст или целиком объяснён — это и есть критерий.

## D. Регрессии и прогоны

`npm test`, `npm run build`, `npm run verify` (включая `tools/selftest_warnings.py` — 13 веток предупреждающих классов). Плюс то, чего прогоны не видят: клиппинг при 1100/1440/1920 и EN, `prefers-reduced-motion`, поведение поповера у нижней границы, фокус после смены главы.

## Формат отчёта

`docs/audit/verdicts/codex-acceptance-wave-c-260807.md`, первая строка:

```
ВОЛНА C: принято X/5 блоков · отклонено Y (адреса) · «магазин» N из 14 (было 5) · немецкого в EN осталось: M строк
```
