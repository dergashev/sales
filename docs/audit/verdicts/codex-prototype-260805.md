ВЕРДИКТ: ОТКЛОНЕНО · чисел сверено 66, расхождений 13 · OBS-проверок 20, провалов 7

# Аудит работающего прототипа S1–S6 · 05.08.2026

## Метод

Оригиналы приложения не изменялись. Репозиторий был чист до записи этого вердикта. Для независимого SSR репозиторий скопирован в `/tmp/attack-05.YpFDUj`, там добавлены только аудитные точки входа для начального состояния и скрытых состояний вкладок/стадий. `renderToString` выполнен для S1–S6, всех пяти вкладок S2, глав 2/3/4 S3, стадий S5 `compose / preflight / confirm / gesendet / zugestellt` и включённой скидки. Получено 20 HTML-снимков в `/tmp/task05-ssr/`.

Базовые команды:

| Проверка | Результат |
|---|---|
| `npm test` | PASS · 3 файла, 50/50 тестов |
| `npm run typecheck` | PASS |
| `npm run build` | PASS · 407 модулей |
| `python3 tools/build_fixtures.py --check` | PASS · производные JSON актуальны |
| `npm run verify` | **FAIL** · одно новое нарушение в самом задании, `docs/audit/verdicts/TASK-05-prototype-audit.md:39-42`; это не дефект прототипа, но baseline не зелёный |
| временный SSR-набор | PASS · 5/5 |
| временный набор поведения store | PASS как воспроизведение наблюдаемого поведения · 8/8 |

Счёт 66 включает каждое денежное, площадное, счётное, процентное и временное значение в проверенных HTML-состояниях; не включает номера вкладок/глав, ID, страницы документов и номера KG. Повторно показанное число считается отдельным экранным утверждением.

## 1. Числа против единственной фикстуры

| Экран / состояние | Значение в HTML | Значение `synthetic-fixtures.md` | Чисел | Сходится |
|---|---|---|---:|:---:|
| S1 | `± 22 %` | `± 22 %` | 1 | да |
| S2 P2 · подтверждённые фикстурой значения | `2.000,00 m²`; `400,00 m²`; `0,00 m²`; WFL `1.500,00 m²` в поле и в конфликте; кандидат `1.560,00 m²`; ставки `≈ 2.545 → ≈ 2.447 €/m²`; `16` WE; `5` Vollgeschosse; текущая ставка `≈ 2.545 €/m²` | §4 и §4 `DEMO-CONF-0001` | 11 | да |
| S2 P2 · Balkon-Anrechnung | `25 %` | значения `25 %` нет; §5 прямо говорит, что в сценарии балконов нет | 1 | **нет** |
| S2 P2 · GK-неопределённость | `Δ ±3 %` | в фикстуре есть `GK 5 · Prüfung erforderlich`, но числовой Δ для GK не объявлен | 1 | **нет** |
| S2 P3 | `± 22 % → ± 13 %`; `− 5` и `− 4` Prozentpunkte; риск `+ 4 %` на KG 320 | §5 `DEMO-SC-02` | 5 | да |
| S2 P5 | `≈ 3.818.000 €`; `≈ 3.915.000 €`; `≈ 3.342.000 €` | Basis / EH 40 / Ohne UG | 3 | да |
| S3 · площади, ставки, срок и разбивка | `2.000,00 m²`; `1.500,00 m²`; `16`; `± 22 %`; `≈ 2.545 €/m² WFL nach WoFlV`; `≈ 1.909 €/m² BGF oberirdisch`; `≈ 238.615 € je Wohneinheit`; `≈ 7,5 Monate`, `19.11.2027`; KG `≈ 2.672.000 / ≈ 840.000 / ≈ 305.000 €`; `70 / 22 / 8 %`; `≈ 3.342.000 €`; `476.000 €` | §5 и §5 «Сроки» | 17 | да |
| S3 · главный итог | текстовое значение `≈ 3.818.000€`: сумма верна, но между числом и `€` нет U+202F, единица вынесена в соседний `<span class="ml-2…">` | `≈ 3.818.000 €` | 1 | **нет** |
| S4 | итоги `≈ 3.818.000 / ≈ 3.342.000 / ≈ 3.915.000`; дельты `− 476.000 / + ≈ 97.000`; ставки `≈ 2.545 / ≈ 2.228 / ≈ 2.610`; BGF UG `400,00 / 0,00 / 400,00` | три прогона Haus A | 11 | да |
| S5 · скидка и preflight | `3,0 %`; `≈ 3.703.000 €`; disclosure `3.703.299,95 €`; `± 22 %` | §5 «Скидка» и `DEMO-SC-02` | 4 | да |
| S6 · Regionalfaktor | `1,08` | §5, Musterland `1,08` | 1 | да |
| S6 · маржа | `12 %`, `6 %` | в фикстуре не объявлены | 2 | **нет** |
| S6 · отношение маржи | `2:1` | в фикстуре не объявлено | 1 | **нет** |
| S6 · риск | `+4␠%`, `+3␠%`, `+2␠%`, `+3␠%`, `+5␠%`, `+3␠%`, где `␠` обозначает фактический U+0020 | объявлено только `+ 4 %` на KG 320; у него в HTML дополнительно неверный U+0020, остальные пять величин отсутствуют | 6 | **нет** |
| S6 · потолок риска | `12 %` | в фикстуре не объявлен | 1 | **нет** |
| **Итого** |  |  | **66** | **53 да / 13 нет** |

Основная арифметика корректна: точный Basis равен `3.817.835,00`, EH 40 даёт точную дельту `97.335,00`, Regionalfaktor — `305.426,80`, скидка считается от точного тотала. Подпись `Zwischensumme der kalkulierten Positionen` полная. UG `476.000 €` законно без `≈`, надземная часть `≈ 3.342.000 €` — с ним.

Каталог ставок действительно читается через `src/state/catalog.ts`; поиск `1545|1.05|1190` вне `src/fixtures` и тестов совпадений не дал. Это не оправдывает отдельные числовые константы S2/S6, которых нет в единственной разрешённой фикстуре.

## 2. Поведение журнала M-4

Прямые вызовы выполнены в изолированном процессе с восстановлением начального состояния перед каждым сценарием.

| Сценарий | Наблюдение | Результат |
|---|---|:---:|
| EH 55 → EH 40 | одно `option.selected`; `deltaExact = 97.335,00`; после undo журнал содержит исходное и обратное события, итог снова `3.817.835,00` | да |
| Повторный undo | второй вызов снова применяет inverse того же EH-события; журнал становится `[option.selected, undo, undo]`, две undo-строки одинаковы | **нет** |
| Невозможность изменения без события | `useStore.setState({ regionalfaktorActive: true })` меняет расчётное состояние при `journal.length === 0` | **нет** |
| Regionalfaktor | `regionalfaktorActive = true`, одно событие, точная дельта `305.426,80` | да |
| Regionalfaktor в снапшоте | флаг есть в текущем Zustand-state, но сущности/сериализации Offer Snapshot в `src` нет; проверить заявленное попадание в снапшот невозможно | **нет** |
| Интервал | начало 22; подтверждение `bgfOber` оставляет 22; WFL даёт 17; подтверждение Energiestandard даёт 13 | да |
| WFL · кандидат клиента | тотал не меняется; ставка `≈ 2.545 → ≈ 2.447`; журнал получает два события | да |
| WFL · альтернатива | state хранит только `wflConflictOpen`; коллекции кандидатов и `selectionStatus=alternative` нет | **нет** |
| Undo конфликта клиента | конфликт снова открыт, но WFL остаётся `1.560,00` и `vom Kunden bestätigt` | **нет** |
| Undo выбора документа | конфликт снова открыт, но provenance документа остаётся `vom Kunden bestätigt` вместо исходного `aus Dokument` | **нет** |

То есть одношаговый happy-path журнала реализован, но утверждение «один журнал делает расхождение невозможным» не выдерживает составного действия и повторного undo.

## 3. Гейты

- **R-07 / MODE-001 — проходит.** В S5 preflight присутствует причина, фокусируемая `aria-disabled="true"` кнопка и отдельное действие следующего шага. HTML: `<span aria-hidden="true">✗ </span>ValidationIssue offen: Klassifikation nach MBO §2 nicht bestätigt…`; `<button … aria-disabled="true" …>Blockiert — Klassifikation bestätigen</button>`; `<button …>Nächster Schritt: Klassifikation bestätigen</button>`.
- **R-18 — проходит.** После одного подтверждения GK итог всё ещё `Zwischensumme der kalkulierten Positionen`, причина содержит `coverage unknown: KG_500`. Только после `setCoverage('KG_500', 'excluded')` результат становится `complete` и label — `Gesamt netto · Grundleistung All3`.
- **EMAIL-001 — структура проходит.** Compose содержит редактируемый `<textarea aria-label="E-Mail-Text">`; затем видимы Preflight, Confirm & Send и Delivery status. Отдельные SSR-состояния содержат `◌ Gesendet — Zustellung ausstehend` и `✓ Zugestellt`.
- **EMAIL-007 / честность симуляции — не проходит.** В исходнике `Zugestellt` автоматически устанавливается таймером через 2,5 секунды после `Gesendet`, хотя UI тут же утверждает, что второй статус не следует из первого. Пользовательская разметка не называет этот переход симуляцией.

## 4. OBS-разметка

| OBS | Проверка | Наблюдение | Итог |
|---:|---|---|:---:|
| 01 | полный label итога | `<p …>Zwischensumme der kalkulierten Positionen</p>` | да |
| 02 | ведущая ставка и норматив | `<p class="numeric …">≈ 2.545 €/m² WFL nach WoFlV</p>` | да |
| 03 | счётный знаменатель и округление | `≈ 238.615 € je Wohneinheit` | да |
| 04 | префиксы над/под землёй | `≈ 3.342.000 €`; `476.000 €` | да |
| 05 | срок и дата рядом | `≈ 7,5 Monate`; `Fertigstellung 19.11.2027` | да |
| 06 | U+202F число–единица | S3: `3.818.000<span class="ml-2…">€</span>`; S6: `+4␠%`, где `␠` — U+0020 | **нет** |
| 07 | интерактив — native control | кликабельная карточка S1 рендерится как `<article class="… cursor-pointer …">`; обработчик находится на `article`, а не на button/link | **нет** |
| 08 | hit target ≥44 px | NumericField: `<input … style="min-height:var(--size-control-visual-md)">`; токен равен 40 px, псевдо-hit-area отсутствует | **нет** |
| 09 | статус не только цветом | видимы `✓`, `▲`, `✗`, `◌` и текстовые подписи | да |
| 10 | overflow таблиц | все таблицы S2–S6 находятся в `<div class="… overflow-x-auto">` | да |
| 11 | caption таблиц | у двух `<table>` S6 нет ни одного `<caption>` | **нет** |
| 12 | roving tabindex S2 | один tab имеет `aria-selected="true" tabindex="0"`, четыре — `false/-1`; проверено на всех вкладках | да |
| 13 | класс `numeric` | денежные и удельные ячейки/герои имеют `numeric`, таблицы выравнивают их вправо | да |
| 14 | raw hex | во всех 20 SSR-снимках найдено 0 raw hex | да |
| 15 | акцентный текст | `text-text-display-accent` найден только на герое №1 S3; body использует белую default surface | да |
| 16 | блокер и следующий шаг | причина и действие присутствуют одновременно, не только tooltip | да |
| 17 | видимые стадии доставки | Compose / Preflight / Confirm & Send / Delivery status; Gesendet и Zugestellt имеют разные текст/знак | да |
| 18 | заглушки названы заглушками | PDF-презентация не помечена `Muster`; доставка показывает фактическое `Zugestellt`, а слово «симуляция» есть только в комментарии исходника | **нет** |
| 19 | Annahmen дословно из T0 | UI заявляет `Texte stammen wörtlich`, но обе клиентские строки сокращены; GK дополнительно заменён с `GK 4` источника на `GK 5` | **нет** |
| 20 | runtime-copy без шаблонных литералов | глава 2 рендерит `KG${NNBSP}100`; глава 4 — `EH${NNBSP}55` | **нет** |

Итого OBS: 20 проверок, 7 провалов.

## 5. Дефекты с адресами

1. **P0 · публичный Zustand API обходит журнал.** Экспорт `useStore` позволяет `setState` без `apply`: `src/state/store.ts:163`. Это прямо опровергает контракт в `src/state/store.ts:12-20`.
2. **P0 · undo не ведёт позицию отмены.** Поиск всегда выбирает последнее не-undo событие, поэтому повторяет один inverse: `src/state/store.ts:379-390`.
3. **P0 · разрешение WFL не является атомарно обратимым.** Customer-path сначала создаёт `value.confirmed`, затем `conflict.resolved`, а inverse второго только открывает флаг: `src/state/store.ts:316-338`. Document-path так же не восстанавливает provenance: `src/state/store.ts:323-337`.
4. **P0 · непринятый кандидат не хранится альтернативой.** Модель содержит только `wflConflictOpen`, но не кандидатов/provenance/selectionStatus: `src/state/store.ts:87-112`; после решения UI-обещание в `src/screens/S2Vorbereitung.tsx:191-195` не обеспечено состоянием.
5. **P1 · заявленного Offer Snapshot нет.** S6 утверждает, что Regionalfaktor попадает в snapshot, `src/screens/S6Einstellungen.tsx:34-38`, но в модели store есть только текущий boolean, `src/state/store.ts:87-117`; поиск `snapshot|Snapshot` в `src` находит лишь fixture-ID и это заявление.
6. **P0 · доставка автоматически выводится из отправки и выдана за факт.** Таймер меняет `gesendet → zugestellt` без delivery receipt: `src/screens/S5Export.tsx:202-207`; фактический label и противоречащее ему объяснение — `src/screens/S5Export.tsx:215-225`.
7. **P1 · PDF/почта не названы заглушками.** `Angebotspräsentation (PDF)` единственная не получает badge `Muster`, `src/screens/S5Export.tsx:21-28` и `src/screens/S5Export.tsx:91-93`; preflight называет выбранное вложение `clientSafe`, `src/screens/S5Export.tsx:160-166`, хотя генератора PDF в `src` нет. README объявляет симуляцию парсинга, но не эти две операции: `README.md:1-3`.
8. **P1 · клиентские Annahmen не дословны и противоречат источнику.** Заявление о дословности — `src/screens/S2Vorbereitung.tsx:372-380`. GK-текст сокращён и меняет `GK 4` на `GK 5`: `src/screens/S2Vorbereitung.tsx:350-359` против `docs/product/t0-fallback-rules.md:106`. KG 500 сокращён: `src/screens/S2Vorbereitung.tsx:361-369` против `docs/product/t0-fallback-rules.md:245`.
9. **P1 · экранные числа вне фикстуры.** Balkon `25 %` и GK `Δ ±3 %`: `src/screens/S2Vorbereitung.tsx:208` и `src/screens/S2Vorbereitung.tsx:220-228`. Маржа, отношение, пять лишних рисков и cap: `src/screens/S6Einstellungen.tsx:49-109`. Значения захардкожены непосредственно в экране.
10. **P1 · сломан U+202F.** Главный итог отделяет `€` CSS-margin вместо символа: `src/screens/S3Konfigurator.tsx:209-215`. Все шесть risk-rate используют U+0020: `src/screens/S6Einstellungen.tsx:90-102`.
11. **P1 · шаблонный литерал попадает в UI и accessible name.** `replace` получает строку `'${NNBSP}'` вместо переменной: `src/screens/S3Konfigurator.tsx:163-170` и `src/screens/S3Konfigurator.tsx:189-196`.
12. **P1 · кликабельная карточка S1 не имеет семантики и клавиатурного пути.** `article` имеет `onClick`, но не role, tabIndex, key-handler или hit-area: `src/screens/S1Projektliste.tsx:35-38`.
13. **P1 · NumericField имеет только 40 px.** Input использует visual-md и не использует `HIT`: `src/components/primitives.tsx:143-160`; visual-md = 40 px, нормативный target = 44 px: `design-system/tokens.css:275-281`.
14. **P1 · таблицы S6 без caption.** Таблица маржи: `src/screens/S6Einstellungen.tsx:53-69`; таблица риска: `src/screens/S6Einstellungen.tsx:80-105`.
15. **Baseline, не прототип.** `npm run verify` падает на обычных пробелах в действующем задании: `docs/audit/verdicts/TASK-05-prototype-audit.md:39-42`. По протоколу этот файл не изменялся.

## 6. Что без браузера не проверено

- вычисленные CSS-размеры и фактическая геометрия 44×44, baseline единиц, переносы, обрезание и горизонтальный scroll на реальных viewport;
- фактические контрасты после cascade, белая поверхность под героем и загрузка трёх начертаний Visuelt Pro;
- клавиатурный focus order, ArrowLeft/ArrowRight после hydration, screen-reader announcements и доступность причин блокировки;
- анимация count-up, delta-chip, `prefers-reduced-motion` и отсутствие layout shift;
- реальные click/type-переходы React. Состояния SSR создавались аудитными начальными значениями в `/tmp`, а обработчики проверялись чтением исходника и прямыми вызовами store;
- clipboard API и поведение таймера доставки в браузере;
- PDF, письмо, delivery receipt и Offer Snapshot проверить невозможно: соответствующих реализаций/выходных файлов в прототипе нет.

Для ручной проверки владельцу в первую очередь нужны: клавиатурный маршрут S1/S2/S3, hit-box NumericField, responsive-таблицы, computed contrast героя и фактический переход S5 через 2,5 секунды.
