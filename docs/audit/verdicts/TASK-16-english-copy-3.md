# Задание Codex № 16 — англоязычная копия, поставка № 3 (остаток партий + каталог опций)

**Черновик. Передавать после TASK-15.**

**Скопируйте текст ниже целиком в Codex.**

---

Ты работаешь в каталоге `/Users/nikitamakarenko/Documents/All3 Execution/Sales Platform 2.0`. Зоны — `PROTOCOL.md` §2-bis. Формат, глоссарий и правила — те же, что в заданиях № 07 и № 14; это третья поставка той же серии.

## Объём — три источника

1. **Остаток экранных строк: `docs/audit/i18n-remainder-260806.md`** (перегенерирован после партий 1–3 ремедиации, 108 строк). Это тексты, созданные при перестройке под настоящую модель Option и «магазин»: CTA-цепочка, «корзина», глава 7 Baugrund & Erschließung, отклики действий, строки состояния Gantt. Ложные срабатывания — как раньше: в отчёт, не в перевод.

2. **Каталог опций из фикстуры `src/fixtures/derived-prototype.json`** — ~118 строк: `label` и `question` каждой группы KG 300/400/сертификатов, `label` и `basis` каждого варианта. Прежние поставки читали только `.tsx`, и каталог — самое заметное немецкое пятно на EN-экране: плитки опций. Ключи — префикс `catalog.`, колонка `de` — дословно значение из JSON. Мост `translateText` подхватит их без изменения кода: подписи каталога уже идут через `tx()`.

3. **Миграция локального дополнения**: `LOCAL_TEXT_EN` в `src/i18n/index.ts` держит ~35 строк хрома, добавленных Claude между поставками (переводы чернового качества). Забери их в поставку своими формулировками — дополнение после этого сократится до нуля, и источником EN снова будет только поставка.

## Формат

`docs/audit/verdicts/content/i18n-en-3-260806.md`, та же таблица `key · de · en · где · числовое · длина`. Ключи не пересекаются с №№ 07/14 (сборщик валит дубль). U+202F между числом и единицей. Термины — по глоссарию № 07; конфликт формулировок с прежними поставками — в отчёт с предложением канона.

## Формат отчёта

`docs/audit/verdicts/codex-i18n-en-3-260806.md`, первая строка:

```
ПОСТАВКА 3: ключей N · не подлежит переводу M · заменено локальных строк K · конфликтов: X
```
