ПОСТАВКА 3: ключей 193 · не подлежит переводу 51 · заменено локальных строк 31 · конфликтов: 5

# Англоязычная копия · поставка №3

Поставка записана в `docs/audit/verdicts/content/i18n-en-3-260806.md`. Все ключи имеют
статус `draft` по `LOCALE-001`.

## Состав

| Источник | Результат |
|---|---:|
| Остаток экранного chrome | 54 ключа покрывают 57 переводимых строк инвентаря; пять S3-фрагментов собраны в два целых сообщения |
| Миграция `LOCAL_TEXT_EN` | 21 новый ключ + 10 уже существующих ключей поставок №1/2 покрывают все 31 локальную строку |
| Каталог KG 300 | 66 ключей |
| Каталог KG 400 | 34 ключа |
| Каталог сертификатов | 18 ключей |
| **Итого новых ключей** | **193** |

Каталог покрыт по заданному контракту полностью: `label` и `question` каждой группы,
`label` и `basis` каждого варианта. Немецкие значения всех 118 строк дословно совпадают
с `src/fixtures/derived-prototype.json`, включая исходную ASCII-транслитерацию.

## Не подлежит переводу

`M=51` — число строк 108-строчного инвентаря, как в отчёте поставки №2:

- 4 — бренд и уже английские имена продукта/сущности;
- 9 — фрагменты кода, типов и комментариев, ошибочно захваченные extractor;
- 10 — значения `KeyboardEvent.key`;
- 8 — имена component API на Foundations;
- 2 — identity-значения `Standard` и `Option`;
- 15 — фрагменты двух нормативных Annahmen;
- 2 — фрагменты тела клиентского письма;
- 1 — имя проекта из фикстуры.

Точные адреса и арифметика приведены в поставке, раздел «Не переводится».

## Конфликты

`X=5` — число строк локального override, расходящихся с уже установленным каноном.

| de | локальный черновик | канон после поставки №3 | основание |
|---|---|---|---|
| `Leistungen KG 300` | `Services CG 300` | `KG 300 services` | `KG` — неизменяемый DIN 276 identifier |
| `Technik KG 400` | `Building services CG 400` | `KG 400 technical systems` | `KG` неизменно; `technical systems` уже используется в поставке №2 |
| `Baunebenkosten KG 700` | `Incidental costs CG 700` | `Ancillary construction costs · KG 700` | глоссарий №07: `Baunebenkosten` → `ancillary construction costs`; `KG` неизменно |
| `Ausbau & Technik` | `Fit-out & services` | существующий `Fit-out & systems` | точный ключ `s3.chapter.fitoutSystems` уже есть в поставке №1 |
| `Vergleichsbasis` | `comparison base` | существующий `Comparison baseline` | глоссарий №07 и ключ `s4.role.comparisonBase` |

Каноническими предлагается считать правую колонку: она сохраняет нормативные IDs и
не создаёт вторых имён для уже определённых сущностей.

## Handoff исполнителю

Поставка не меняет чужие зоны. Для подключения нужны следующие адресные действия:

1. `tools/build_i18n.py:24-27` — добавить
   `docs/audit/verdicts/content/i18n-en-3-260806.md` в `SOURCES`, затем пересобрать
   `src/i18n/generated.ts`.
2. `src/i18n/index.ts:101-136` — удалить все 31 записи `LOCAL_TEXT_EN`; после сборки
   21 строка приходит из новых ключей, десять — из поставок №1/2. Сам local-first
   lookup также больше не должен перекрывать канонические переводы.
3. `src/screens/OptionChapter.tsx:148-150` — `label` уже проходит через `tx()`, а
   `basis` пока вставляется напрямую; пропустить `c.basis` через тот же мост.
   `src/screens/OptionChapter.tsx:169-174` — аналогично перевести façade axes.
4. `src/screens/S3Konfigurator.tsx:136-149` — `Card` сейчас выводит `title`/`intro`
   напрямую. Пропустить оба через `tx()`, чтобы целые ключи главы 7 на строках 515-548
   действительно использовались.
5. `src/components/ScheduleGantt.tsx:73-95`, `src/screens/S4Vergleich.tsx:173-180` и
   `src/screens/S5Export.tsx:140-155,185-198,223-233` — оставшийся raw chrome нужно
   подключить к `tx()`; английские формулировки и целые ключи уже находятся в поставке.

## Проверки

- 193 строки таблиц успешно разобраны тем же regex-контрактом, что использует
  `tools/build_i18n.py`;
- распределение: `54 + 21 + 66 + 34 + 18 = 193`;
- 118/118 каталоговых значений `de` совпадают с JSON как multiset; пропусков и лишних
  строк нет;
- все 31 уникальные строки `LOCAL_TEXT_EN` покрыты объединением поставок №1–3;
- дубликатов key между поставками №1, №2 и №3 — 0;
- в поставке №3 нет U+00A0; немецкое `3 %` использует U+202F, en-GB — `3%`;
- `npm run verify` — PASS: новых нарушений 0; 7 известных открытых нарушений и
  предупреждения репозитория не относятся к поставке №3;
- `git diff --check` — PASS.
