# Задание Codex № 19 — англоязычная копия, поставка № 4 (guidance и доменные строки)

**Черновик. Передавать после TASK-18.**

**Скопируйте текст ниже целиком в Codex.**

---

Ты работаешь в каталоге `/Users/nikitamakarenko/Documents/All3 Execution/Sales Platform 2.0`. Зоны — `PROTOCOL.md` §2-bis. Схема поставки та же, что в заданиях № 07, 14 и 16.

## Повод — твоя же таблица из приёмки № 17

Ты прошёл EN по золотому пути и разложил остаток по четырнадцати поверхностям на три природы: **chrome**, **guidance** и **product/domain data**. Хром закрыт поставками № 1–3 (словарь 784 ключа). Это задание закрывает то, что осталось, — но не всё подряд, потому что две из трёх природ имеют РАЗНЫЙ статус по нормативу, и смешивать их нельзя.

## Что переводится: всё, что видит пользователь

**Решение PO от 07.08 (`D-24`), дословно:** «Англоязычная версия должна быть на английском, немецкая — на немецком. Это всё, что нужно знать.»

`D-20`, разрешавший оставлять guidance немецким, **отменён**. Вопроса «переводить ли подсказки» больше нет — переводится всё, что пользователь читает на экране:

1. **Оставшийся хром.** Свежий инвентарь: перегенерируй `python3 tools/i18n_remainder.py`.
2. **Guidance целиком** — коучинг-подсказки, intro глав, объяснения методики, тексты гейтов и причин блокировок. Раньше это был fallback по D-20; теперь это долг перевода.
3. **Доменные подписи интерфейса** — названия артефактов, подписи покрытия, статусы, объяснения провенанса, тексты Annahmen, подписи драйверов, тексты рисков (`Risikozuschlag`, категории, средства снятия — новое в `D-23`).

**Что решение НЕ отменяет — не перепутай:**
- `LOCALE-001` в силе: ключи остаются `draft`, клиентские артефакты на EN заблокированы до утверждения. Это про артефакты, не про интерфейс.
- `D-13` в силе: язык артефакта — отдельная настройка. Немецкий PDF при английском интерфейсе законен; **немецкая кнопка при английском интерфейсе — нет.**
- Нормативные немецкие имена (`WoFlV`, `DIN 276`, `MBO §2`, `GEG`, `KG 320`) остаются немецкими: это идентификаторы, а не текст.

Прототип сейчас честно помечает EN черновиком. **Твоя поставка обязана сделать эту пометку ненужной** — после неё немецкого в интерфейсе при `uiLanguage = en` остаться не должно, кроме нормативных имён.

## Формат

`docs/audit/verdicts/content/i18n-en-4-260807.md` — та же таблица `key · de · en · где · числовое · длина`; ключи не пересекаются с №№ 07/14/16 (сборщик валит дубль); U+202F между числом и единицей; глоссарий прежний.

## Формат отчёта

`docs/audit/verdicts/codex-i18n-en-4-260807.md`, первая строка:

```
ПОСТАВКА 4: ключей N · guidance переведено: да · немецкого в EN-интерфейсе осталось: M строк · конфликтов: X
```

`M` — не ноль по умолчанию: назови поимённо каждую строку, которая остаётся немецкой, и причину (нормативное имя / значение фикстуры / содержимое артефакта по D-13). Список станет предметом следующей приёмки, а пометка «EN · Entwurf» снимется ровно тогда, когда он окажется пуст или целиком объяснён.
