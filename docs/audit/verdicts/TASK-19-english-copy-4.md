# Задание Codex № 19 — англоязычная копия, поставка № 4 (guidance и доменные строки)

**Черновик. Передавать после TASK-18.**

**Скопируйте текст ниже целиком в Codex.**

---

Ты работаешь в каталоге `/Users/nikitamakarenko/Documents/All3 Execution/Sales Platform 2.0`. Зоны — `PROTOCOL.md` §2-bis. Схема поставки та же, что в заданиях № 07, 14 и 16.

## Повод — твоя же таблица из приёмки № 17

Ты прошёл EN по золотому пути и разложил остаток по четырнадцати поверхностям на три природы: **chrome**, **guidance** и **product/domain data**. Хром закрыт поставками № 1–3 (словарь 784 ключа). Это задание закрывает то, что осталось, — но не всё подряд, потому что две из трёх природ имеют РАЗНЫЙ статус по нормативу, и смешивать их нельзя.

## Что переводится, а что нет — разберись до перевода

1. **Chrome, ещё оставшийся немецким.** Переводится. Свежий инвентарь — `docs/audit/i18n-remainder-260806.md` (перегенерируй `python3 tools/i18n_remainder.py`, файл датируется автоматически).

2. **Guidance (коучинг-подсказки, intro глав, объяснения методики).** По **D-20** EN-guidance **не переводится и не выдумывается**: показывается немецкий fallback. Прежде чем переводить хоть строку guidance, скажи прямо, отменяет ли PO D-20 — сам ты этого решить не можешь, и я тоже. Если D-20 в силе, guidance остаётся немецким, и задание закрывает только пункты 1 и 3.

3. **Product/domain data (подписи итога из движка, Annahmen, драйверы, названия артефактов).** По **D-13** язык артефактов — ОТДЕЛЬНАЯ настройка от языка интерфейса, и в прототипе её нет. Здесь нужен твой вердикт: какие из этих строк на самом деле хром (и переводятся сейчас), а какие — содержимое артефакта (и ждут настройки языка артефактов). Граница проходит не по файлу, а по тому, попадает ли строка в клиентскую выдачу.

Прототип уже честно называет EN черновиком **до** переключения: подпись «EN · Entwurf» и пояснение, что guidance и доменные данные остаются немецкими. Задание должно сократить остаток, а не сделать пометку ложной.

## Формат

`docs/audit/verdicts/content/i18n-en-4-260807.md` — та же таблица `key · de · en · где · числовое · длина`; ключи не пересекаются с №№ 07/14/16 (сборщик валит дубль); U+202F между числом и единицей; глоссарий прежний.

## Формат отчёта

`docs/audit/verdicts/codex-i18n-en-4-260807.md`, первая строка:

```
ПОСТАВКА 4: ключей N · guidance: переведено/оставлено по D-20 · artefakt-data отложено M · конфликтов: X
```

Плюс разнесение остатка по трём природам с адресами — по нему PO примет решение о D-20 и о настройке языка артефактов.
