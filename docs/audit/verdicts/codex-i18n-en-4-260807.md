ПОСТАВКА 4: ключей 91 · guidance переведено: да · немецкого в EN-интерфейсе осталось: 2 строки · конфликтов: 0

# Англоязычная копия · поставка №4

Поставка записана в `docs/audit/verdicts/content/i18n-en-4-260807.md`. Все
91 ключ имеют статус `draft` по `LOCALE-001`. D-24 отменяет немецкий fallback
в интерфейсе, но не разрешает английские клиентские артефакты до утверждения
локали и не связывает язык UI с языком артефакта.

## Состав

| Блок | Ключей |
|---|---:|
| Переходный маркер черновой локали | 3 |
| Гейт клиентского вида DC-33 | 9 |
| InternalNote / AutosaveChip и события | 14 |
| Анализ документов | 4 |
| Оффер, фильтры и сравнение опций | 17 |
| Экспорт и stale-состояние | 3 |
| Причины неполноты из `.ts` | 6 |
| Динамические события и расчётные подписи из `.ts` | 14 |
| Риски, надбавки и события | 19 |
| Два полных текста Annahmen | 2 |
| **Итого** | **91** |

Guidance переведено целиком в границах текущего продукта: прежние guidance-
ключи уже находятся в поставках №1–3, а поставка №4 добавляет новые тексты
гейта, скидки, рисков, следующего шага и два Annahmen, ранее исключённые по
D-20. Динамические данные проверены отдельно в `src/engine/risk.ts`,
`src/i18n/reasons.ts`, `src/state/store.ts`, `src/engine/calculate.ts` и
`src/fixtures/derived-prototype.json`: исходный сканер `.tsx` их не видел.

## Остаток M=2

| строка | класс | причина |
|---|---|---|
| `Musterprojekt Nordfeld` | значение фикстуры | Имя проекта не локализуется. |
| `Sehr geehrte Damen und Herren,\n\nanbei erhalten Sie unser indikatives Angebot für das Musterprojekt Nordfeld.\n\nMit freundlichen Grüßen` | содержимое артефакта | Немецкое тело письма следует отдельному языку артефакта по D-13. Английский клиентский артефакт остаётся заблокирован `LOCALE-001`. |

Нормативные `WoFlV`, `DIN 276`, `MBO §2`, `GEG`, `KG 320`, `GK 5`, `QNG` и
`DGNB` остаются неизменными внутри английских строк. Они являются
идентификаторами, а не немецким fallback. Бренд `All3`, английские
`Opportunities`, `Standard`, `Option`, имена component API и клавиши также
не входят в `M`.

## Разбор свежего инвентаря

`python3 tools/i18n_remainder.py` дал 111 кандидатов в 20 файлах. Регулярное
выражение намеренно грубое:

- реальные новые строки взяты в поставку напрямую либо заменены целыми
  сообщениями с плейсхолдерами;
- фрагменты двух Annahmen сведены в два полных ключа;
- фрагменты intro главы 7 уже покрыты целыми ключами
  `remainder3.ground.intro` и `remainder3.servicing.intro` поставки №3;
- `Escape`, `Tab`, `Enter`, стрелки, `Home`, `End`, фрагменты TypeScript и
  комментариев — не экранная копия;
- имена примитивов на Foundations и `Standard` / `Option` — component API и
  языково нейтральные имена, перевод не требуется;
- три разорванных фрагмента письма сведены в одно содержимое артефакта по
  D-13, указанное в `M`.

## Конфликты

`X=0`. Поставка продолжает канон прежнего глоссария: `KG` и нормативные IDs
не переводятся; `Baugrundgutachten` — `ground investigation report`;
`Baugrund` — `ground conditions`; `Schätzunsicherheit` — `estimate
uncertainty`; `Risikozuschlag` — `risk allowance`. Новых вторых имён для
существующих сущностей не введено.

## Handoff исполнителю

Поставка не меняет чужие зоны. Для подключения нужны адресные действия:

1. `tools/build_i18n.py:24-28` — добавить
   `docs/audit/verdicts/content/i18n-en-4-260807.md` в `SOURCES`, затем
   пересобрать `src/i18n/generated.ts`.
2. `src/App.tsx:178-205` — после подключения снять `EN · Entwurf` и два
   helper-текста: остаток `M` полностью объяснён D-13/fixture и больше не
   означает неполный интерфейс.
3. `src/components/DocumentAnalysis.tsx:167-171`,
   `src/screens/OpportunityList.tsx:77-82,145-150,221-225` и
   `src/screens/S5Export.tsx:302-312` — заменить сборку динамических фраз на
   целые шаблоны поставки с плейсхолдерами.
4. `src/components/InternalNote.tsx:67-106`, `src/state/store.ts:1268-1292`
   — подключить статусы/метаданные и два шаблона журнальных событий.
5. `src/engine/risk.ts:35`, `src/screens/S3Konfigurator.tsx:536-573`,
   `src/state/store.ts:1365-1382` и `src/components/OfferPanel.tsx:337-350`
   — переводить данные риска, remedies, driver label, журнал и delta-chip;
   готовые динамические шаблоны находятся в `domain4.risk.*`.
6. `src/screens/S2Vorbereitung.tsx:421-443` — два Annahmen выводить через
   целые ключи `guidance4.assumption.*`; не переводить фрагменты.
7. `src/i18n/index.ts:9-16,47-51` и `src/screens/S6Einstellungen.tsx:168-181`
   — удалить действующие утверждения D-20 о намеренном немецком fallback;
   D-24 отменил их.
8. `src/i18n/reasons.ts:9-27` — принимать язык/форматтер и собирать все
   причины через `domain4.reason.*`; текущая прямая строка обходит мост.
9. `src/state/store.ts:816-1455`, `src/engine/calculate.ts:220-225` и
   `src/components/OfferPanel.tsx:285-295,337-350,653-666` — хранить у
   события/драйвера message key + params либо форматировать целый шаблон до
   показа. Не применять `translateText()` к уже собранной динамической
   строке. Для старых событий использовать уже поставленные `journal.*`,
   `driver.*`, `money.*` и `schedule.*`; для новых — `domain4.*` отсюда.

## Проверки поставки

- таблицы разбираются regex-контрактом `tools/build_i18n.py`;
- новых ключей: 91; ключей до поставки: 784; пересечений ключей: 0;
- U+00A0 в поставке №4: 0; правило U+202F не нарушено;
- пустых `de`/`en`: 0;
- все ключи используют целые сообщения; у динамических строк явно названы
  плейсхолдеры и адреса подключения;
- `git diff --check` — PASS.
