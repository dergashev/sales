# Задание Codex № 23 — приёмка волны D

**Черновик. Передавать, когда Claude сообщит о заморозке `src/**` (`PROTOCOL.md` §2-ter).**

**Скопируйте текст ниже целиком в Codex.**

---

Ты работаешь в каталоге `/Users/nikitamakarenko/Documents/All3 Execution/Sales Platform 2.0`. Зоны — `PROTOCOL.md` §2-bis. Приёмка: `src/**` только читается. Начни с того же, с чего начал волну C, — зафиксируй `HEAD` и сводный хеш `src/**` до и после прохода: это оказалось лучшей защитой от вердикта по движущемуся объекту.

## A. Три блока, отклонённые волной C

Не «проверь, стало ли лучше», а **проверь механизм**, как в прошлый раз.

1. **Блок 4 — слоты и поповер.** Слот призрака держит высоту при трёхстрочном содержимом; дельта-слот не двигает recap; поповер у нижней границы окна переворачивается вверх. Прошлый провал был точечным: `useLayoutEffect` мерил портал до того, как у него появился измеримый бокс, и не перемерял после монтирования — проверь именно это, при `viewport height 500`.
2. **Блок 5 — новые компоненты.** DC-14 (тур), DC-30, DC-33, DC-43: объявлены ли они в `src/state/data-states.ts`, реализованы ли семь состояний либо названа причина неприменимости; `ClientOutputGateDialog` не держит закрытый диалог в DOM и переводит фокус туда, куда требует контракт; прямой `setMode` в шапке больше не обходит ворота DC-33; `InternalNote` не оставляет пустую секцию в дереве доступности при `praesentation` (`NOTE-006` — заметки не скрыты, а отсутствуют).
3. **Блок 2 — раскладка.** После `TASK-22`: при ширине центральной колонки 300 / 640 / 1240 px плитки не вылезают, изображения не превращаются в полоски.

## B. EN — снятие пометки

Поставка № 4 к этому моменту интегрирована. Твой прошлый замер дал **≥ 202 немецких фрагмента** на живом EN-пути при обещанных двух строках — расхождение на два порядка, и оно важнее самой цифры: значит, инвентарь ловил не то, что видит пользователь.

Поэтому: пройди EN-путь и **назови каждый оставшийся немецкий фрагмент с причиной** (нормативное имя · значение фикстуры · содержимое артефакта по `D-13` · пропуск перевода). Если пропусков нет — пометка «EN · Entwurf» снимается, и это надо сказать прямо. Если есть — их список станет заданием № 24.

**Про расхождение 2 против 202 — это дефект НАШЕГО инструмента, не твоего.** `tools/i18n_remainder.py` написан Claude и читает строковые литералы `.tsx`, то есть измеряет исходник, а не то, что видит пользователь. Твой живой замер оказался прав, и инвентарь приведён к его методу: теперь остаток меряется обходом DOM на английском пути. Твоя задача здесь — независимая проверка, а не объяснение чужого промаха.

## C. Четвёртый UX-замер

Те же 14 поверхностей и та же шкала. Счёты: **3 → 5 → 7 из 14**. Отдельно проверь главы 1 и 3 — обе оставались «налоговой формой» два замера подряд, и Claude работает над ними по твоим же трём исправлениям на каждую: глава 3 — карточки объёма вместо набора `SegmentedControl`, глава 1 — DC-46/47 и человеческие следствия у каждого факта.

## D. Прогоны

`npm test`, `npm run build`, `npm run verify` (включая `tools/selftest_warnings.py`). Плюс живое: `prefers-reduced-motion`, клиппинг на трёх ширинах, фокус после смены главы, поведение поповера у границ.

## Формат отчёта

`docs/audit/verdicts/codex-acceptance-wave-d-260807.md`, первая строка:

```
ВОЛНА D: принято X/3 блоков · «магазин» N из 14 (было 7) · немецкого в EN: M фрагментов · пометка EN снимается: да/нет
```
